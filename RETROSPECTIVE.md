# DayGrid — 회고 (1장)

## AI에게 어떤 일을 맡겼는지
프론트엔드(React 컴포넌트·화면)와 백엔드(Cloudflare Worker 라우팅, D1 스키마, 인증·인가 로직)
코드 작성 전체, 로컬 실행 환경 구성(Vite/Wrangler), 회원가입~로그인~일정 CRUD~권한 검사까지
end-to-end 테스트(curl/Node 스크립트 및 브라우저 클릭 테스트), Git 저장소 초기화와 GitHub 업로드,
그리고 이번 PRD/회고 문서 작성까지 맡겼다.

## 개발 중 가장 큰 문제
로컬 D1(SQLite)을 처음 띄웠을 때 `SQLITE_CANTOPEN` 오류로 서버 자체가 시작되지 않았다.

**원인 추정**: 프로젝트가 만들어진 경로가 매우 깊게 중첩된 임시 작업 폴더 안이라, D1이 실제로
사용하는 로컬 SQLite 파일 경로 길이가 Windows의 경로 길이 제한(260자)을 넘겨버린 것이었다.

**해결 방법**: 원인을 좁히기 위해 실제 경로 길이를 직접 계산해 확인한 뒤, 프로젝트 폴더 전체를
`C:\Users\user\projects\daygrid`처럼 훨씬 짧은 경로로 옮기고 나서야 로컬 DB가 정상적으로
열렸다. 이 경험 때문에 프로젝트를 임시 작업공간이 아니라 처음부터 사용자 소유의 영구적인
폴더에 두는 것이 왜 중요한지 체감했다.

## 다음 버전에서 추가할 기능
- 반복 일정(매주 특정 요일에 자동 등록)
- 완료율 등 간단한 통계 (단, 이번 MVP의 Won't 범위였으므로 의도적으로 미루었음)
- 일정 태그/우선순위

## 3분 데모 순서
1. 서비스 소개 (DayGrid = 개인 일정 캘린더, 카테고리별 색상 구분)
2. 로그인
3. 월간 달력 확인 → 날짜 클릭
4. 일정 등록 (제목/시간/카테고리)
5. 완료 체크 (취소선 표시 확인)
6. 일정 수정
7. 일정 삭제 (확인창 확인)
8. 새로고침 → 데이터가 그대로 남아 있는 것 확인
