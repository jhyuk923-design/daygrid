# DayGrid — PRD (1장 요약)

## 서비스 한 문장 설명
DayGrid는 월간 달력에서 날짜를 선택해 공부·운동·업무·음악·약속·기타 일정을 등록하고,
완료 여부를 체크하는 개인용 캘린더 웹 서비스다.

## 해결하려는 문제
여러 일정이 머릿속이나 여러 앱에 흩어져 있어, 오늘 무엇을 해야 하는지 한눈에 관리하기 어렵다.

## 타깃 사용자
공부·운동·업무·음악·약속 등 개인 일정을 한 화면에서 관리하고 싶은 개인 사용자.

## MVP 핵심 흐름
로그인 → 월간 달력 확인 → 날짜 선택 → 일정 등록 → 등록한 일정 확인 → 완료 여부 변경 →
필요하면 수정 또는 삭제. 이 흐름이 처음부터 끝까지 정상 작동한다.

## Must 기능 (구현 완료)
| # | 기능 |
|---|---|
| 1 | 월간 달력 표시 (이전/다음/오늘 이동) |
| 2 | 날짜 클릭 → 해당 날짜 일정 모달 |
| 3 | 일정 등록 (제목/날짜/시간/카테고리/메모) |
| 4 | 카테고리 6종 (공부/업무/운동/음악/약속/기타) |
| 5 | 등록한 일정 조회 (월 단위) |
| 6 | 완료 / 미완료 변경 |
| 7 | 일정 수정 |
| 8 | 일정 삭제 (확인 다이얼로그 포함) |
| 9 | 새로고침해도 유지되는 저장 (Cloudflare D1, 실제 DB) |
| 10 | 로그인/회원가입/로그아웃, 본인 일정만 조회, 타인 데이터 접근 차단 |

## Won't 기능 (이번 MVP에서 의도적으로 제외)
친구 초대, SNS 기능, 채팅, 복잡한 통계, AI 일정 추천, 푸시 알림, 결제, 네이티브 모바일 앱,
복잡한 반복 일정, Notion 수준의 다중 페이지 시스템.

## 데이터 모델
```
users   : id, name, email, passwordHash, salt, createdAt
sessions: id, userId, expiresAt, createdAt
events  : id, userId, title, date, startTime, endTime, category, memo,
          completed, completedAt, createdAt, updatedAt
```

## 사용한 기술과 선택 이유
| 영역 | 선택 | 이유 |
|---|---|---|
| 프론트엔드 | React + Vite | 컴포넌트 단위 개발이 이해하기 쉽고 설정이 단순 |
| 백엔드 | Cloudflare Workers | 완전 무료, 프론트 빌드 결과물까지 같은 Worker가 서빙(CORS/쿠키 문제 없음) |
| DB | Cloudflare D1 (SQLite) | Worker와 같은 생태계, 새로고침 후에도 남는 실제 저장소 |
| 인증 | 자체 구현 (Web Crypto PBKDF2 해시 + salt + HttpOnly 세션 쿠키) | 표준적인 해시 방식으로 보안 요건을 충족하면서, 이미 구현·테스트가 끝나 별도 서비스 연동 없이 바로 배포 가능 |
| 배포 | Cloudflare Workers (단일 배포) | `wrangler deploy` 한 번으로 프론트+백엔드+DB가 함께 배포됨 |

## 완료 기준 (Definition of Done)
- [x] 일정 등록 후 새로고침해도 사라지지 않는다
- [x] 완료/미완료 상태가 새로고침 후에도 유지된다
- [x] 일정 수정·삭제가 정상 동작한다
- [x] 다른 사용자의 일정에는 접근할 수 없다 (404)
- [x] 빈 제목 등 잘못된 입력을 서버가 거부한다 (400)
