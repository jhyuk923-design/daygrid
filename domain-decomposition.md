# 자기 도메인 업무 분해 예시 — DayGrid

## [도메인 맥락]
나는 **개발 입문자** 도메인 **1년 차 미만**이고, **개인 일정·공부 기록을 관리하는 작업**을 자동화/정리하고 싶습니다.
(이번 과정에서 실제로 만든 결과물이 개인 캘린더 서비스 **DayGrid**다.)

## [네 동작 분석]

1. **분해 (Decomposition)**
   "캘린더 앱을 만든다"는 하나의 큰 목표를, 서로 독립적으로 만들고 검증할 수 있는 작은 단위로 나눴다.
   - 회원가입 / 로그인 / 로그인 유지 / 로그아웃 (인증)
   - 월간 일정 조회
   - 일정 생성 · 수정 · 삭제 · 완료 토글 (CRUD)
   - 카테고리별 색상 구분, 검색·필터
   - 다른 사용자의 데이터 접근 차단 (인가)

2. **패턴 인식 (Pattern Recognition)**
   일정 조회·생성·수정·삭제·완료토글, 다섯 개 API가 전부 **"로그인 확인 → 입력값 검증 → 이 자원이 내 것인지 확인 → DB 접근 → JSON 응답"** 이라는 똑같은 순서를 반복한다는 걸 알아챘다. 그래서 이 반복 패턴을 미들웨어(`middleware/auth.js`)와 공통 검증 함수(`utils/validate.js`)로 한 번만 만들어 다섯 곳에서 재사용했다.

3. **추상화 (Abstraction)**
   세부 구현(D1에 보낼 SQL 문법, 비밀번호 해시 계산 방식, 쿠키 문자열 조립 규칙 등)은 함수 뒤로 숨기고, 나머지 코드는 "일정 하나 = 제목·날짜·시작시간·종료시간·카테고리·메모·완료여부"라는 하나의 단순한 개념 모델만 다루면 되게 만들었다. 프론트엔드도 데이터가 실제로 Cloudflare D1에 저장된다는 사실을 몰라도 되도록 `services/api.js` 뒤에 감췄다.

4. **알고리즘 (Algorithm)**
   "달력을 연다 → 그 달 데이터만 한 번의 요청으로 가져온다 → 날짜별로 묶어서 그린다 → 검색·필터는 이미 받아온 데이터 안에서 처리한다"는 정확한 순서를 정해서, 하루에 날짜마다 30번씩 요청을 보내는 비효율을 피했다.

## [출력 요구]
마크다운 문서 형식으로, 한국어로.

## [검증 기준]
**개발을 전혀 모르는 사람**이 읽어도 **"이 앱이 왜 이렇게 나뉘어 설계되었는지 이해할 수 있다"**고 평가할 것.
